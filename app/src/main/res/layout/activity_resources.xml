<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/ivLogoSmall"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:contentDescription="Logo"
            android:layout_gravity="center"
            android:src="@drawable/chier_minds_logo"/>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Resources"
            android:textSize="20sp"
            android:textStyle="bold"
            android:gravity="center"
            android:layout_marginTop="12dp"/>

        <!-- Example resource items, replace with real data or RecyclerView -->
        <Button
            android:id="@+id/btnResource1"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="Download: Self-help guide"
            android:layout_marginTop="16dp"/>

        <Button
            android:id="@+id/btnResource2"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="Download: Mental health workbook"
            android:layout_marginTop="12dp"/>

        <!-- Add more items or implement RecyclerView for real list -->
    </LinearLayout>
</ScrollView>